# CLI `ct-lab`

A CLI do CT Lab permite operar sem o desktop:

```bash
ct-lab session    # sessão interativa
ct-lab run        # executa comando único
ct-lab --help     # ajuda completa
```

> A CLI usa o mesmo `ct-mcp-server` que o desktop. Configure o provider via `CT_PROVIDER` e `CT_MODEL`.
