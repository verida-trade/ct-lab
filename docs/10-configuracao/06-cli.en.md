# CLI `ct-lab`

CT Lab's CLI allows operating without the desktop:

```bash
ct-lab session    # interactive session
ct-lab run        # single command
ct-lab --help     # full help
```

> The CLI uses the same `ct-mcp-server` as the desktop. Configure provider via `CT_PROVIDER` and `CT_MODEL`.
